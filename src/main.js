import { createApp } from 'vue'
import simulator from './Simulator.vue'
import IPX from "@/IPX";
import dearpygui from "@/dearpygui";
createApp(dearpygui).mount("#ppt")

//createApp(simulator).mount("#ppt")
// createApp(IPX).mount("#ppt")
